<li>
  <%= image_tag secondary_image.image.thumb, id: "img-preview" %>
  <%= form_for [@product, secondary_image], :method => :delete do |form| %>
  <hr style="margin:4px; border-color:rgba(80,80,80,0.4)">
    <%= form.submit "DELETE", class: "btn btn-danger btn-xs centerize" %>
  <% end %>
</li>
