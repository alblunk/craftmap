<div class="<%= size %>">
	<h6 class="centerize" style="font-size:.6em !important;pointer-events:none;font-weight:600;letter-spacing:.05em !important;"><%= brand.name%></h6>
  <%= link_to image_tag(brand.logo), brand_path(brand),class: 'thumbnail' %>
  <h6 class="centerize" style="font-size:.5em !important;pointer-events:none;margin-top:-18px"><%= brand.location%></h6>

</div>
